export interface SpotifyResponse {
  ranking: string;
  status: string;
  artist_and_title: string;
  weeks: string;
  peak: string;
  streams: string;
  streams_delta: string;
  total_streams: string;
}
